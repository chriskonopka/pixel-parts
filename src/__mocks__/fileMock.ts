// Returns a plain string for static asset imports (images, fonts, etc.)
// so that tests can render components that import binary files without errors.
const fileMock = 'test-file-stub';
export default fileMock;
